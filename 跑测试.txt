请你基于我当前项目已有的正式 1fold 输出，不重新训练模型，只使用已有的 valid_signals.csv 和 test_signals.csv 做交易规则对照实验。

目标：
验证当前策略是否被 buy 信号拖累，以及 sell_only + 更高分位阈值是否更合理。

输入文件位置假设为：
outputs/diagnostics/basic_deep_full_1fold/walk_forward/fold_000/models/auc_weighted_ensemble/valid_signals.csv
outputs/diagnostics/basic_deep_full_1fold/walk_forward/fold_000/models/auc_weighted_ensemble/test_signals.csv

如果实际 run_name 不同，请自动搜索：
Get-ChildItem -Recurse outputs\diagnostics -Filter test_signals.csv
并优先选择 auc_weighted_ensemble 目录下的 test_signals.csv。

请完成以下工作：

1. 不重新训练模型。
2. 读取 valid_signals.csv 和 test_signals.csv，index 解析为 datetime。
3. 使用 valid_signals 决定阈值，不允许用 test_signals 选阈值，避免未来函数。
4. 测试以下交易规则组合：

- both_q95_q95:
  trade_direction_mode = both
  buy_quantile = 0.95
  sell_quantile = 0.95

- both_q98_q98:
  trade_direction_mode = both
  buy_quantile = 0.98
  sell_quantile = 0.98

- sell_only_q95:
  trade_direction_mode = sell_only
  buy disabled
  sell_quantile = 0.95

- sell_only_q98:
  trade_direction_mode = sell_only
  buy disabled
  sell_quantile = 0.98

- buy_only_q95:
  trade_direction_mode = buy_only
  buy_quantile = 0.95
  sell disabled

- buy_only_q98:
  trade_direction_mode = buy_only
  buy_quantile = 0.98
  sell disabled

5. 如果当前 risk_management.py 的 run_a_share_inventory_t0_backtest 还不支持 trade_direction_mode，请临时在测试脚本里复制一个轻量版本或包装 test_signals：
   - sell_only：禁止 buy_first 开仓，只允许 sell_first 开仓；sell_first 后仍允许 buy_prob 触发买回。
   - buy_only：禁止 sell_first 开仓，只允许 buy_first 开仓；buy_first 后仍允许 sell_prob 触发卖出。
   - both：保持原逻辑。
   注意：不要改动训练模型。

6. 回测参数使用项目 config 默认值：
   initial_capital = 1_000_000
   base_position_pct = 0.6
   max_t0_trade_pct = 0.3
   min_position_pct = 0.25
   max_position_pct = 1.0
   position_sizing_high_quantile = 0.99
   commission_rate = 0.001
   slippage_rate = 0.0005
   tp = 0.003
   sl = 0.002
   min_lot_size = 100
   max_participation_rate = 0.05
   a_share_t0_mode = inventory

7. 每个规则组合输出：
   - total_return
   - alpha_total_return
   - alpha_max_drawdown
   - alpha_sharpe
   - alpha_sortino
   - alpha_calmar
   - trade_count
   - win_rate
   - avg_net_return_per_trade
   - median_net_return_per_trade
   - buy_threshold
   - sell_threshold
   - buy_quantile
   - sell_quantile
   - trade_direction_mode

8. 同时保存每个组合的：
   - trades CSV
   - equity curve CSV
   - stats JSON

9. 输出目录：
outputs/diagnostics/rule_compare_from_existing_signals/

建议文件结构：
outputs/diagnostics/rule_compare_from_existing_signals/
  rule_compare_summary.csv
  rule_compare_summary.json
  both_q95_q95/
    t0_trades.csv
    t0_equity_curve.csv
    t0_backtest_stats.json
  both_q98_q98/
    ...
  sell_only_q95/
    ...
  sell_only_q98/
    ...
  buy_only_q95/
    ...
  buy_only_q98/
    ...

10. 最后请在终端只打印 rule_compare_summary.csv 的简短表格，不要输出大段 JSON。

11. 完成后请把以下文件返还：
outputs/diagnostics/rule_compare_from_existing_signals/rule_compare_summary.csv
outputs/diagnostics/rule_compare_from_existing_signals/rule_compare_summary.json

如果能一起返还最优组合的 t0_trades.csv 和 t0_backtest_stats.json 更好。